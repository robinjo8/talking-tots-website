import { Button } from "@/components/ui/button";
import { LogOut } from "lucide-react";
type FooterSectionProps = {
  handleSignOut: () => void;
};
export function FooterSection({
  handleSignOut
}: FooterSectionProps) {
  return <div className="mt-8 text-center text-sm text-muted-foreground">
      
      
      
    </div>;
}